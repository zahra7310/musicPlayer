<template>
  <form>
    <label
      for="default-search"
      class="mb-2 text-sm font-medium text-gray-900 sr-only dark:text-white"
      >Search</label
    >
    <div class="relative w-60 ">
      <div
        class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none "
      ></div>
      <input
        type="search"
        v-model="search"
        id="default-search"
        class="block w-full p-4 pl-2 text-sm text-white border border-gray-500 rounded-lg bg-gray-700 focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500"
        placeholder="Search Podcast, Music..."
        required
      />
      <button
        @click="searchTrack"
        class="text-white absolute right-2.5 bottom-2.5 bg-gray-900 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"
      >
        <svg
          class="w-4 h-4 text-white dark:text-gray-400"
          aria-hidden="true"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 20 20"
        >
          <path
            stroke="currentColor"
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="m19 19-4-4m0-7A7 7 0 1 1 1 8a7 7 0 0 1 14 0Z"
          />
        </svg>
        <!-- <img src="/src/assets/images/icons/search-64.png"> -->
      </button>
    </div>
  </form>
</template>


<script>
export default {
  data() {},
  methods: {
    searchTrack(e) {
      let searchResult=[]
       e.preventDefault();
       for(let i=0;i<this.$store.state.podcasts.length;i++){
        if(this.$store.state.podcasts[i].name==this.search){
          this.searchResult = this.$store.state.podcasts[i];
        }
    }
    if(this.searchResult){
      console.log(this.searchResult);
      return searchResult;
    }else{
      console.log('not found');
    }
  },
}
}
</script>
